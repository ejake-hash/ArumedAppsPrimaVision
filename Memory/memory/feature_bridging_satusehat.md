---
name: feature-bridging-satusehat
description: "Plan bridging Satu Sehat (4 resource utk statistik dashboard), koneksi sandbox TERUJI, keputusan scope, gap data, fase termasuk dashboard monitoring"
metadata: 
  node_type: memory
  type: project
  originSessionId: 35466789-517e-4994-9e64-a1621bafb9bf
---

Bridging **Satu Sehat (SATUSEHAT, FHIR R4 Kemenkes)** untuk Arumed — BEDA dari [[feature-bridging-vclaim]] (itu BPJS/HMAC; ini Kemenkes/OAuth2 FHIR).

**Tujuan user (spesifik):** kirim 4 FHIR resource agar muncul di **statistik dashboard Satu Sehat**: Jumlah Kunjungan=`Encounter`, Jumlah Diagnosis=`Condition`, Jumlah Peresepan=`MedicationRequest`, Obat Dibawa Pulang=`MedicationDispense`. + dashboard monitoring di dalam Arumed.

**KONEKSI SANDBOX TERUJI 2026-05-29** (via curl, HTTP 200 semua):
- OAuth2 token OK (`status: approved`, `expires_in` ~14399 dtk di sandbox ≈4 jam, prod ~1 jam). POST `{oauth}/accesstoken?grant_type=client_credentials`, body form-urlencoded client_id+client_secret.
- GET Organization OK ("STAGING firenzaa7@gmail.com").
- Resolve Patient by NIK → IHS Number OK: `GET {fhir}/Patient?identifier=https://fhir.kemkes.go.id/id/nik|{NIK}`.
- Base sandbox: `https://api-satusehat-stg.dto.kemkes.go.id/oauth2/v1` & `/fhir-r4/v1`. Prod: `https://api-satusehat.kemkes.go.id/...`.
- Kredensial sandbox di `backend/.env` (gitignored): org_id `378de02d-43a9-486b-beee-9181d9346972`. **Secret pernah terkirim via chat → WAJIB regenerate utk production.**

**Kerangka SUDAH ADA (tinggal isi):** `SatusehatService` (placeholder/mock semua builder+postFhir), tabel `satusehat_sync_logs`+`satusehat_resource_logs`, kolom `visits.satusehat_encounter_id/sync_status/synced_at`, 5 endpoint `/integrasi/satusehat/*`, row IntegrationConfig SATUSEHAT, `DashboardService::getSatusehatStatus`.

**Keputusan user (disepakati):**
- Kode obat **KFA** (Kamus Farmasi & Alkes, kode obat standar nasional) → **tambah kolom `medications.kfa_code`** + helper cari KFA. MedicationCodeableConcept system `http://sys-ids.kemkes.go.id/kfa`.
- **Tambah `employees.nik`** (Employee TIDAK punya nik, cuma nip/sip/str) utk resolve Practitioner IHS.
- Trigger = **batch harian (scheduler 23:59 WIB) + tombol Sync Manual**.

**Gap data ditutup (migration):** `employees.nik`+`employees.satusehat_ihs`, `medications.kfa_code`, `patients.satusehat_ihs` (cache IHS). Patient SUDAH punya `nik`; diagnosa dari `doctor_examinations.diagnosis_utama`+`diagnosis_sekunder[]` (ICD-10); resep `prescriptions`+`prescription_items`; dispense `prescriptions.status=DISPENSED`+`dispensed_at`.

**6 fase (BERTAHAP):** 1=Auth+TestConn (`Satusehat/SatusehatClient` OAuth+cache token), 2=migrasi+resolve IHS, 3=Encounter+Condition, 4=MedicationRequest+Dispense, 5=scheduler+log, **6=Dashboard Monitoring** ⭐ (`GET /integrasi/satusehat/dashboard` + sub-view `BridgingSatusehatView.vue`).

**Frontend modul `/bridging/*` SUDAH ADA** di `arumed-frontend/src/views/bridging/` (BridgingLayout+nav: Konfigurasi/VClaim/Antrean/Log). Header layout SUDAH menyebut "Satu Sehat" tapi belum ada tab/halaman → dashboard = tambah 1 tab + 1 sub-view (murah, konsisten). RBAC `integrasi.read`/superadmin.

**EKSEKUSI DIMULAI 2026-05-30 — FASE 1+2 SELESAI, FASE 3 BUILDER SIAP (belum kirim).** Plan lengkap: `Docs/PLAN-BRIDGING-SATUSEHAT-ARUMED.md`.

- **Koreksi keputusan (2026-05-30):** (1) `kfa_code` TETAP di `medications` (master) — UI muncul di menu **Inventori Farmasi → master Obat** (view fisik views/master-data/, router reroute); dispense baca via `$item->medication->kfa_code` (relasi sudah dipakai FarmasiService). BUKAN di inventory_stocks (itu cuma batch/qty, item_id→medications.id). (2) Kredensial via UI Konfigurasi (encrypted), BUKAN .env/seeder. (3) **Location DIKOSONGKAN dulu** (Encounter SKIP field location bila kosong); helper auto-register Location (POST /Location) ditunda SETELAH koneksi lulus. (4) `kfa_code` DITUNDA ke Fase 4.

- **FASE 1 SELESAI + koneksi LIVE (user aktifkan via UI).** `App\Services\Satusehat\SatusehatClient` (pola BpjsClient): OAuth2 client_credentials + token cache (TTL expires_in-60), oauthBase/fhirBase per-env (base_url=HOST + suffix /oauth2/v1, /fhir-r4/v1), org_id/loc_id boleh di credentials ATAU configuration, 2 guard (`assertEnabled` toggle+cred vs `assertHasCredentials` utk Test-sebelum-Aktif). `testConnection()` nyata. Seeder SATUSEHAT diisi base_url sandbox + config non-secret. UI `BridgingKonfigurasiView` +blok SATUSEHAT (Environment select + Org ID + Location ID + Client ID/Secret write-only); Test disabled sampai has_credentials.
- **FASE 2 SELESAI (tanpa kfa_code).** Migration `2026_05_31_000011_add_satusehat_nik_ihs_columns` (employees.nik+satusehat_ihs, patients.satusehat_ihs). Fillable Employee/Patient. `resolvePatientIhs/resolvePractitionerIhs` (cache kolom→GET identifier nik→entry[0].resource.id→save), `probeIhsByNik` uji. **RESOLVE LIVE TERUJI**: Patient NIK `9999999999999999`→IHS `P02029536465`. NIK asli→null (normal sandbox, di-skip aman). NIK Practitioner dummy belum ketemu—**user mencarikan dari Postman/dok sandbox**.
- **FASE 3 TUNTAS — Encounter+Condition TERKIRIM SUNGGUHAN ke sandbox 2026-05-30 (HTTP 200, keduanya 201 Created).** Encounter id contoh `f0a151ca-653c-4622-bcb4-784d8bf7799a`, Condition `aea27a0a-...`. Statistik Kunjungan+Diagnosis NAIK di dashboard Satu Sehat.
  - **TEMUAN PENTING dari validasi Kemenkes (Encounter):** WAJIB 3 elemen yg dulu dikira opsional — `Encounter.identifier` (RuleNumber 10117, system `http://sys-ids.kemkes.go.id/encounter/{org_id}`, value=no_registrasi/no_antreen/id), **`Encounter.location` WAJIB** (10120 — Location TIDAK opsional!), `Encounter.diagnosis` (10457, refer ke Condition).
  - **Pakai FHIR transaction Bundle** (`buildVisitBundle`+`syncVisitBundle`): POST ke root `{fhir}/`, Condition+Encounter dalam 1 transaksi, Encounter.diagnosis refer Condition via `urn:uuid:` (memecah dependensi melingkar). `buildConditionPayloads($visit, requireEncounter:false)` utk jalur Bundle.
  - **Location auto-register JADI (helper #17 selesai):** `registerLocation(name, physicalType='ro', saveToConfig)` → POST /Location → UUID → simpan ke config.location_id. Sudah daftar "Poliklinik Mata" → `d96972fb-65d2-4633-ac44-5d6eebdc6abd`.
  - **Format response Satu Sehat (beda dari FHIR standar):** hasil di `entry[].response` (BUKAN entry[].resource), field `resourceID`+`resourceType`+`status` ("201 Created")+`location` header. `extractEncounterId` & `logBundleEntries` baca dari sana.
  - **Idempoten by-identifier:** server tolak duplikat (RuleNumber 20002 `code:duplicate`) bila identifier sama sudah terkirim. `syncVisitBundle` deteksi duplicate→`findEncounterIdByIdentifier` (GET Encounter?identifier=...)→simpan→anggap SUKSES. + skip POST bila visit.satusehat_encounter_id sudah ada.
  - **NIK uji TERUJI:** Practitioner NIK `1271161411980001`→IHS `10010456878`. Patient NIK dummy `9999999999999999`→IHS `P02029536465`. **GOTCHA: patients.nik UNIQUE** — 1 NIK dummy cuma bisa utk 1 pasien (tes sandbox terbatas 1 visit; produksi pakai NIK asli masing2).
- **SISA**: Fase 4 (Medication+kfa_code), Fase 5 (scheduler+log), Fase 6 (dashboard). `syncVisit()` lama (5-resource per-POST + ImagingStudy) masih ada TAPI usang—batchSync sebaiknya pindah ke `syncVisitBundle`.
- **NIK dokter UI DITUNDA**: DataPenggunaView tak punya editor field employee (hanya akun/role; profession/nip read-only). bpjs_dpjp_code diedit di BpjsMappingModal tab Kode DPJP (pakai setDpjpCode skalar). Pakai NIK uji dulu; nanti endpoint setNik TERPISAH (jangan campur setDpjpCode BPJS).
- **Build hijau + smoke 35/35** sepanjang Fase 1-3.
- ⚠️ **cacert**: dev Windows cURL error 60 → [[feedback-php-ssl-cacert-windows]] (restart SEMUA proses php wajib — banyak proses lama bisa numpuk).
- ⚠️ **Write-tool path bug**: di `d:\apps- cl` (spasi di `apps- cl`), **Write tool file BARU → lokasi hantu** yang PHP/PS/Bash tak lihat (lint "Could not open input file"). **Edit file existing OK**. Fix: file baru lewat Bash `cat > heredoc`. Migration `000010` gagal gini → ditulis ulang `000011`.

**FASE 4 SELESAI 2026-05-30 — 4 RESOURCE STATISTIK SEMUA TERKIRIM SUNGGUHAN (201 Created):** Encounter+Condition+Medication+MedicationRequest+MedicationDispense dalam 1 Bundle, HTTP 200. Statistik #1-4 (Kunjungan/Diagnosis/Peresepan/Obat-pulang) NAIK di dashboard Satu Sehat.
- Migration `2026_05_31_000012_add_kfa_code_to_medications_table` (medications.kfa_code) + fillable Medication. Migrated.
- **Obat butuh resource Medication TERPISAH** (Satu Sehat tolak medicationCodeableConcept di MedReq/Disp — RuleNumber 10135/10138 medicationReference WAJIB). `buildMedicationEntries($visit,$encUuid)` hasilkan per item ber-KFA: Medication + MedicationRequest + MedicationDispense(jika DISPENSED), saling refer via urn:uuid. Ganti `buildMedicationRequestPayloads`/`buildMedicationDispensePayloads` lama.
- **TEMUAN VALIDASI Kemenkes (obat):** (a) `Medication.extension` WAJIB (10031) — medicationType url `https://fhir.kemkes.go.id/r4/StructureDefinition/MedicationType`, valueCodeableConcept code `NC` (non-racikan), system `http://terminology.kemkes.go.id/CodeSystem/medication-type`. (b) MedReq/Disp WAJIB `identifier` (10388/10389) — system `http://sys-ids.kemkes.go.id/prescription/{org}` (system `/dispense/{org}` DITOLAK — pakai /prescription utk dispense juga). (c) MedDisp WAJIB `authorizingPrescription`→MedicationRequest (10393). (d) `quantity` WAJIB UCUM valid: **Satu Sehat tolak annotation `{tablet}`/`{drop}`/`{botol}` & kode `1`/`{tbl}`/`[iU]`**. Yang OK: `mL`,`mg`,`g`,`ug`,`[drp]`,`[arb'U]`. Mapper `ucumCode($unit)` → default fallback `[arb'U]` (arbitrary unit, verified OK).
- **KFA via API Kemenkes**: endpoint `{host}/kfa-v2/products/all?product_type=farmasi&keyword=X` (HTTP 200, butuh `product_type`). Cendo Xytrol→KFA `93004665`. Ini basis fitur "Cari KFA" UI (item #13).
- **SKIP-aman**: item tanpa kfa_code dilewati (tak gagalkan visit). MedDisp hanya utk prescription status=DISPENSED.
- _Data uji diubah (sandbox): obat Cendo Xytrol kfa_code=93004665 (tetap), resep visit demo→DISPENSED+dispensed_by (tetap, berguna demo), no_registrasi dipulihkan REG-20260528-001._

**FASE 5 SELESAI 2026-05-30 (scheduler + batch).** `batchSync`/`syncVisit`/`retry` DIALIHKAN ke `syncVisitBundle` (jalur tunggal Bundle; method per-POST send*/sendResource/builder singular DIHAPUS sbg dead code). Command `satusehat:batch-sync` (+`--retry`) di `app/Console/Commands/SatusehatBatchSync.php` (no-op bila integrasi off). Scheduler `routes/console.php`: 23:59 batch + 01:00 retry (timezone Asia/Jakarta, withoutOverlapping). `retryLatestUnfinished()` utk --retry. batchSync eager-load relasi + reset PENDING saat retry. Verified: command jalan; visit hari ini FAILED krn pasien tanpa IHS + dokter NULL (skip-aman benar, bukan bug — produksi pakai NIK asli).
**FASE 6 SELESAI 2026-05-30 (dashboard monitoring).** Backend: `dashboardStats($from,$to)` → connection + 4 cards (SUCCESS/FAILED per resource dari resource_logs) + visits ringkas + trend 7 hari + readiness (dokter tanpa NIK / obat tanpa KFA / pasien tanpa IHS) + batches. Passthrough `IntegrasiService::satusehatDashboard` + controller `satusehatDashboard` + route `GET /integrasi/satusehat/dashboard`. Frontend: `views/bridging/BridgingSatusehatView.vue` (banner koneksi + 4 stat-card + date-range + kesiapan data + tren bar CSS + Sync Manual + tabel batch + Retry) + tab "Satu Sehat" di BridgingLayout (icon health, urutan: Konfigurasi/VClaim/Antrean/**Satu Sehat**/Log) + route `/bridging/satusehat` + `integrasiApi.satusehatDashboard/SyncManual/Retry/SyncLog/ResourceLog`. Build hijau + smoke 35/35.

**UI kfa_code + Cari KFA + CSV SELESAI 2026-05-30:**
- CSV: REGISTRY obat `columns` +`kfa_code` (setelah `code`; `code` di-exclude auto-gen, kfa_code IKUT header). csvTemplateNotes obat +baris KFA. Export+import+template round-trip TERUJI (import `kfa_code` tersimpan).
- Backend Cari KFA: `SatusehatClient::kfaSearch(keyword)` (hit HOST `/kfa-v2/products/all?product_type=farmasi`, di LUAR /fhir-r4) + `SatusehatService::searchKfa` (ringkas kfa_code/name/nama_dagang/dosage_form) + passthrough `IntegrasiService::satusehatSearchKfa` + controller `satusehatKfaSearch` + route `GET /integrasi/satusehat/kfa-search?keyword=`. TERUJI: "xytrol"→4 hasil (93004665 CENDO XITROL Tetes Mata).
- Frontend: `MasterFormModal` +dukungan generik `field.action:{label}` (tombol di samping input text, emit `field-action`) — REUSABLE non-invasif. `_CrudResourceView` +emit `field-action` (forward) + expose `setModalField(key,value)`. `ObatView`: field kfa_code (action "Cari KFA") + kolom KFA + modal pencarian KFA (cari→pilih→setModalField). integrasiApi.satusehatKfaSearch. Build hijau + smoke 35/35.

**UI NIK dokter SELESAI 2026-05-30 — SELURUH INTEGRASI SATU SEHAT KELAR.** Backend: `IntegrasiService::setEmployeeNik` (set employees.nik + RESET satusehat_ihs bila NIK berubah → re-resolve) + controller `setEmployeeNik` + route `PUT /integrasi/satusehat/dokter/{employeeId}/nik`. `UserService::format` +nik+satusehat_ihs di employee (prefill frontend). Frontend DataPenggunaView: field "NIK Dokter (Satu Sehat)" di modal user, MUNCUL hanya saat role=dokter & employee_id ada (`canSetNik`); kalau dokter tanpa employee → note "hubungkan employee dulu". Simpan via `integrasiApi.setEmployeeNik` SETELAH saveUser bila NIK berubah (employee_nik_saved deteksi perubahan). TERUJI: set NIK→resolve IHS 10010456878→ganti NIK→IHS reset null→clear. Build hijau + smoke 35/35.

**Kartu Status BPJS & Integrasi di AdmisiView (2026-05-29):** `AdmisiService::getDashboard()` query `IntegrationConfig` utk `bpjs_status` — TADINYA cuma VCLAIM/ANTREAN/ICARE (SATUSEHAT hilang → kartu Admisi selalu "Satu Sehat Offline" via fallback hardcoded FE). FIX: tambah `'SATUSEHAT'` ke whereIn (ICARE tetap, difilter keluar di FE AdmisiView.vue:191). Kartu kini tampil status Satu Sehat asli (is_enabled+last_test_status). Fallback `ok:false` FE jadi pasif. Smoke 35/35.

**UI KELOLA LOKASI SELESAI 2026-05-30.** Backend SatusehatService: `listLocations` (GET /Location?organization= → items{id,name,status,physical_type} + active_id dari config.location_id), `registerLocation` (sudah ada), `updateLocation` (PUT full resource), `deactivateLocation` (FHIR TAK hard-delete → PUT status=inactive; bila yg dinonaktifkan sedang aktif → kosongkan config), `setActiveLocation`. Passthrough IntegrasiService + controller + 5 route (`/satusehat/location` GET/POST, `/{id}` PUT/DELETE, `/{id}/active` PUT — **route `/active` SEBELUM `/{id}`**). Frontend BridgingKonfigurasiView: tombol "🗺️ Kelola Lokasi" di kartu SATUSEHAT (bawah Location ID) → modal Teleport (daftar lokasi + status + tandai Aktif + inline edit + nonaktifkan + form daftar baru). api.js satusehatLocations/Register/Update/Delete/SetActive. TERUJI sandbox: register→UUID, update nama, deactivate inactive, location aktif tak terganggu (register set_active=false). Org sandbox ternyata sudah punya 12 lokasi terdaftar. Build hijau + smoke 35/35.
- ⚠️ **Bug fixed**: `UserService::getAll/getById` whitelist eager-load employee `id,name,profession,nip` TIDAK termasuk nik → prefill NIK Data Pengguna gagal. Ditambah `nik,satusehat_ihs` ke whitelist.

**STATUS: BRIDGING SATU SEHAT LENGKAP TOTAL (Fase 1-6 + UI KFA + UI NIK + UI Kelola Lokasi).** 4 statistik terkirim sungguhan, scheduler harian, dashboard monitoring, UI kfa_code+Cari KFA, UI NIK dokter, UI Location (daftar/edit/nonaktif/set-aktif). Yg tersisa hanya operasional produksi: isi NIK dokter asli + KFA obat asli (atau CSV) + ganti kredensial production.

⚠️ **Catatan tooling**: Write tool file BARU **frontend Vue OK**; gagal cuma backend PHP di path `d:\apps- cl` (pakai Bash `cat`). Bash `cat heredoc` TIDAK cocok utk file penuh quote (Vue) — pakai Write tool. patients.nik & (NIK) unique.
- _Fase 5 (batch & log):_ 11. scheduler `batchSync('AUTO')` 23:59 + retry 01:00 (routes/console.php). 12. BridgingLogView tampilkan sync-log Satu Sehat + drill resource-log + Retry. 13. (opsional) tombol "Cari KFA".
- _Fase 6 (dashboard, permintaan khusus user):_ 14. endpoint `GET /integrasi/satusehat/dashboard` (`dashboardStats`). 15. `BridgingSatusehatView.vue` (4 stat-card + status koneksi + kesiapan data + tren + Sync Manual + Retry). 16. tab "Satu Sehat" di BridgingLayout + route `/bridging/satusehat`.
- _Ditunda (di luar fase):_ 17. helper auto-register Location (POST /Location) — koneksi sudah lulus, bisa kapan saja. 18. UI NIK dokter (endpoint `setNik` terpisah). 19. ImagingStudy/Observation = di luar scope.

**Catatan akurasi:** ImagingStudy butuh DICOM → DI LUAR scope 4 statistik. Idempoten: simpan resource id, jangan double-POST. Encounter dulu baru resource lain (butuh encounter ref). Obat tanpa kfa_code → SKIP (logged), jangan gagalkan visit.
