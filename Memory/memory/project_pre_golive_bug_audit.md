---
name: project-pre-golive-bug-audit
description: "Audit E2E pra-go-live 2026-05-29 — 9 bug ditemukan & SEMUA DIPERBAIKI + di-commit di branch fix/pre-golive-500-bugs. Bug #2 bukan consent tapi FormRegistry finalize."
metadata: 
  node_type: memory
  type: project
  originSessionId: 38e4e63a-a273-4408-a98c-b9c849873ced
---

Audit E2E pra-go-live dijalankan 2026-05-29 (DB Postgres hidup, semua migrasi Ran, frontend build hijau, 0 error compile). Inti sistem TERBUKTI SEHAT lewat 26 test PASS: queue state machine, gate TR→DOKTER (finalize-AND), RBAC 403 guard, auth JWT, billing consolidation (consolidateBilling OK, invoice+total valid). **Finalize otomatis advance**: `finalizeAssessment`/`finalizeRefraction` panggil `checkReadyForDoctor()` → bikin antrian DOKTER saat 2 gate lolos; frontend sengaja TIDAK panggil `/selesai` (lihat komentar perawatStore.js:104) → TIDAK ada double-advance.

**STATUS: SEMUA 9 BUG SUDAH DIPERBAIKI & DI-COMMIT 2026-05-29 di branch `fix/pre-golive-500-bugs` (BELUM merge ke main).** Commits: 311d963 (snapshot 198 file + bug #1-6), 6b479ae (smoke test), 4e48616 (#7), 607e225 (#8), 3c34fc9 (#9), 6abcda7 (BedahDemoSeeder). Fix per-bug ditandai ✅ FIX di bawah. #1-6 ditemukan via sweep endpoint/scan statis; #7-9 ditemukan via E2E alur + seeding.

**9 bug ditemukan (#1-6 fase awal, #7-9 fase E2E alur & seeding):**

1. 🔴 `GET /asuransi/klaim` 500. `AsuransiService.php` indexKlaim eager-load `'invoice:id,invoice_number,total_amount'` tapi `billing_invoices` TIDAK punya `total_amount` (namanya `total`). Frontend tab "Daftar Klaim" TIDAK baca kolom itu (cuma loaded, tak dirender) → ganti aman. Scan presisi 43 eager-load → cuma ini salah. **✅ FIX: `total_amount` → `total`. Re-test 200.**

2. ⚠️ **DIAGNOSIS AWAL SALAH** — BUKAN consent. Consent (`AdmisiService::saveConsentDocument`) SUDAH AMAN (inline `gz=null` lines 757-758, TIDAK panggil FormRegistryService). Error consent di log 14:45 itu dari test run-ku sendiri sebelum paham. Bug ASLI: `FormRegistryService::finalize` (line 178-180) `gzcompress()` → tulis ke kolom `rendered_html_gz` (bytea) via binding string → Postgres tolak `byte 0x9c invalid UTF8`. Path ini dipanggil `POST /rekam-medis/document/{id}/finalize` (RekamMedisController:420, dipakai TtdDokumen/Form Registry). Dibuktikan via bytea_probe: tulis gz binary langsung MEMANG throw. Data: dok ~2.5KB, 0 dok pakai gz → plain lossless. **✅ FIX: samakan pola AdmisiService (simpan plain rendered_html, gz=null). getSnapshot sudah fallback. Re-test plain-write roundtrip OK.** Lihat [[feature-form-registry]].

3. 🟢 route yatim — `GET+POST /dokter/jadwal-bedah` 500: method `indexJadwalBedah`/`storeJadwalBedah` TIDAK ADA di DokterController, TANPA konsumen frontend. Penjadwalan bedah ditangani `DokterService::resolveSurgerySchedule` (auto saat finalize) + `bedahSlot`. **✅ FIX: hapus 2 route yatim. Re-test 404 (bukan 500).**

4. 🟢 route yatim — `GET /klaim/vclaim-log` 500 = DUA bug berlapis: (a) route ordering `/klaim/{id}` sebelum `/vclaim-log` → diparse UUID; (b) `KlaimService::getVclaimLog` raw `DB::table('bpjs_vclaim_logs')->whereNull('deleted_at')` tapi tabel tak ada deleted_at. **✅ FIX: pindah route statis (vclaim-log/icare/grouping-log) di atas `/{id}` + hapus whereNull deleted_at. Re-test 200.**

5. 🟡 `GET /dashboard/satusehat-status` 500: `DashboardService::getSatusehatStatus` raw `DB::table('satusehat_sync_logs')->whereNull('deleted_at')` tapi tabel tak pakai SoftDeletes. **✅ FIX: hapus baris whereNull. Re-test 200.** (Scan: cuma #4 & #5 yang punya pola raw whereNull deleted_at salah.)

6. ⚪ LATENT — `PerawatService::storeAssessment` line 156 baca `$data['has_allergy']` tanpa `??` → crash kalau absen. Tak terjangkau via API (validator `required|boolean`). **✅ FIX: `($data['has_allergy'] ?? false) ? ... : null`.**

7. 🔴 **BUG#7 (ditemukan 2026-05-29 sesi E2E alur)** — Dispensing farmasi baca/decrement kolom legacy `medications.stock` (mostly 0 pasca-redesign inventori) padahal stok asli di `inventory_stocks` per-batch → dispensing GAGAL "stok tidak cukup" walau stok fisik ADA (inventory_stocks=5601, medications.stock=0). **✅ FIX (commit 4e48616): InventoryStockService tambah public `onHand()` + `consume()` FEFO (lockForUpdate, abort 422); FarmasiService::selesaiDispensing & siapkanSurgeryRequest (BHP) pakai itu. Verified dispensing 5 unit → inventory_stocks 101→96.** SISA belum diubah (perlu keputusan terpisah): endpoint manual `updateStokObat`/`adjustStock` masih edit kolom legacy `stock` — di LUAR alur dispensing. Lihat [[feature-inventori-farmasi]]. **LANJUTAN 2026-05-29 [[feature-stok-per-lokasi]]: dispensing/BHP `onHand`+`consume` kini PER-LOKASI (FARMASI/BEDAH, strict) — `inventory_stocks` dapat kolom `location`; deliver request unit = transfer gudang→unit.**

8. 🔴 **BUG#8 (dilaporkan user "tidak ada antrean di Bedah" 2026-05-29)** — `/bedah/antrian` bocorkan baris zombie (visit=NULL/patient=NULL) dari visit yg sudah di-cancel/soft-delete → tampil baris kosong serba "—" di UI. `BedahService::getPatientQueue` tak `whereHas('visit')` (AdmisiView sudah punya, station lain BELUM). **Audit: 6 stasiun lain (Penunjang/Farmasi/Kasir/Perawat/Refraksi/Dokter) gap sama.** **✅ FIX (commit 607e225): tambah `->whereHas('visit')` di getPatientQueue ke-7 service. Dokter: superadmin tadinya tak terlindungi (dokter biasa implisit via whereHas visit.doctorSchedule). Verified /bedah/antrian 0 baris (zombie tersaring), 6 lain 200, smoke 35/35. Data zombie TIDAK dihapus, cukup difilter (non-destruktif).** PELAJARAN: E2E test tadi cuma cek `current_station`, TIDAK cek output endpoint `/bedah/antrian` — user yg nangkap. Test E2E harus verifikasi apa yg endpoint UI kembalikan, bukan cuma state visit.

9. 🔴 **BUG#9 (ditemukan saat seeding farmasi 2026-05-29)** — `DokterService::storePrescription` (line 413-416) tulis `dose/frequency/route/duration_days` ke `PrescriptionItem`, TAPI kolom itu TAK ADA di `prescription_items` (cuma `dosage/instructions`) DAN tak fillable → Eloquent diam-diam buang. Aturan pakai obat dari dokter HILANG total; apoteker terima item tanpa instruksi. Lolos di E2E sebelumnya krn non-fillable di-skip tanpa error. Frontend DokterView KIRIM 4 field terpisah (jumlah/signa/posisi/durasi) + baca-balik utk prefill → map ke instructions LOSSY. **✅ FIX (commit 3c34fc9): migration `2026_05_31_000010` tambah kolom dose/frequency/route/duration_days + PrescriptionItem fillable. Verified tersimpan utuh.**

**SEED DEMO (commit 6abcda7)**: `BedahDemoSeeder` idempoten, demo-only (TIDAK di chain). Jalankan: `php artisan db:seed --class=BedahDemoSeeder`. Isi: 3 obat(MED-90x)+3 BHP(BHP-90x)+3 IOL master, inventory_stocks per-batch (480 unit), tarif UMUM+BPJS (procedure/bhp/iol), Paket Phaco composite (PKG-PHACO, 5 item+2 tarif), + 4 pasien hari ini: BEDAH (Siti Aminah), FARMASI (Budi Santoso, resep 2 item), KASIR (Dewi Lestari, invoice 2 item), TTD (Andi Wijaya, template DEMO_TTD_DOKTER PENDING_SIGNATURE). **Gotcha skema**: medications/bhp pakai kolom legacy `stock` (isi 0, stok riil di inventory_stocks); IOL pakai brand/model/iol_type (no code/name); surgery_package_tariffs butuh `classification`; TTD masuk antrean dokter HANYA jika template field_schema punya field `signature_canvas` signer_type=`doctor` (SignatureService::ttdQueueForDoctor); prescription_items kolom = dosage/instructions + (baru) dose/frequency/route/duration_days.

**E2E FUNGSIONAL 4 alur (2026-05-29, service layer DB dev) — 15/16 PASS**: Penunjang D→P→D BENAR (order→`kirimKePenunjang` pause `DI_PENUNJANG`→hasil finalize→requeue anti-duplikat reuse 1 baris→`SELESAI_PENUNJANG`); Bedah same-day D→BEDAH→KASIR BENAR (planning=BEDAH+`surgery_date`=today bikin schedule); Bedah terjadwal hari lain D→KASIR (bukan BEDAH) BENAR; Farmasi K→F routing BENAR, dispensing = BUG#7 (fixed). **Gotcha test**: method dokter tab4=`storePlanning` (bukan storeTab4), resep=`storePrescription`; field jadwal=`surgery_date` (bukan scheduled_date); kirim ke penunjang=`kirimKePenunjang` (pause, BUKAN advanceFromStation yg complete); login sbg DOKTER pemilik schedule (ownership 403); `diagnosis_utama` wajib di storePlanning; medication_id resep wajib (FK).

**File yang diubah (semua lint hijau)**: bug#1-6 → AsuransiService, DashboardService, KlaimService, FormRegistry/FormRegistryService, PerawatService, routes/api.php; #7 → FarmasiService + InventoryStockService; #8 → 7 service getPatientQueue; #9 → migration 2026_05_31_000010 + PrescriptionItem. **DI-COMMIT di branch `fix/pre-golive-500-bugs` (BELUM merge main)**: 311d963 (snapshot 198 file + bug#1-6), 6b479ae (smoke test), 4e48616 (#7), 607e225 (#8), 3c34fc9 (#9), 6abcda7 (BedahDemoSeeder).

**SMOKE TEST permanen (commit 6b479ae)**: `backend/tests/Smoke/EndpointSmokeTest.php` + testsuite `Smoke` di phpunit.xml + script `composer smoke`. Sweep 30+ READ-endpoint + 3 endpoint eks-500, assert non-5xx + auth 401 + route yatim 404. **Jalan terhadap DB DEV Postgres (TIDAK RefreshDatabase, READ-ONLY)** krn env tak punya pdo_sqlite + bug Postgres-specific. Token via `JWTAuth::fromUser` (tidak ubah auth global). Set DB via `config()` + `DB::purge('pgsql')` di setUp krn phpunit.xml override DB_DATABASE=:memory:. 35/35 hijau, terbukti nangkap regресi (tes Bug #1 sengaja dikembalikan → suite gagal presisi). Prasyarat: DB dev hidup + seed. PHPUnit 12 → pakai atribut `#[DataProvider]`, bukan `@dataProvider`.

**BUKAN bug (sudah diverifikasi)**: `/klaim/icare/monitoring` 503 = sengaja (iCare belum aktif); `/asuransi/verifikasi` 405 = POST-only; error `medical_equipment relasi tidak ada` (log 04:32) & `23502 not-null` (log 08:17) = STALE dari sesi lalu, sudah fixed (model $table='medical_equipments' sudah benar).

**Tooling gap pra-live**: tidak ada ESLint (frontend) & minim feature test (backend). MAYORITAS dari 9 bug sekelas "data/nama kolom/route/method drift" — beberapa SILENT (tulis ke kolom/relasi yg tak ada → Eloquent buang tanpa error: consent gzip #2, dispensing stock #7, prescription dose #9). Kelas silent ini lolos justru karena tak error; ketangkap lewat E2E/seeding, bukan smoke read-only. Rekomendasi: ESLint + perluas E2E yg cek PERSISTENSI data (bukan cuma status code).

**Catatan teknis**: doc arsitektur (skill) bilang login return `access_token`, tapi backend+frontend pakai `token` (app konsisten, doc usang). PowerShell `Invoke-RestMethod` mangle bearer header → pakai curl/bash untuk test auth, BUKAN PS.

Terkait: [[feature-insurance-tpa]] (modul asuransi, bug #1 di sini), [[feature-form-registry]] (gzip storage, bug #2), [[dokterview-module]] (bug #3).
